<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="default_dialog_style">
        <item name="android:windowFrame">@null</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowIsFloating">true</item>
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:backgroundDimAmount">0.7</item>
        <!--显示区域以外是否使用黑色半透明背景-->
        <item name="android:backgroundDimEnabled">true</item>
    </style>

    <!-- windowEnterAnimation 进入的动画代码里面-->
    <style name="main_menu_animStyle">
        <item name="android:windowEnterAnimation">@anim/anim_btm_top</item>
        <item name="android:windowExitAnimation">@anim/anim_top_btm</item>
    </style>

    <!-- windowEnterAnimation 进入的动画代码里面-->
    <style name="top_bottom">
        <item name="android:windowEnterAnimation">@anim/anim_top_btm</item>
        <item name="android:windowExitAnimation">@anim/anim_btm_top</item>
    </style>

    <!--  弹框样式    带半透明遮罩层  -->
    <style name="dialog_style">
        <item name="android:windowFrame">@null</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowIsFloating">true</item>
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:backgroundDimAmount">0.7</item>
        <!--显示区域以外是否使用黑色半透明背景-->
        <item name="android:backgroundDimEnabled">true</item>
    </style>

</resources>